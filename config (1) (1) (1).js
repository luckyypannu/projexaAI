/**
 * config.js — TrustTrace Frontend Configuration
 *
 * FIX: This file was listed in the project spec for Member 4 but was never
 * created. api.js was hardcoding BASE_URL directly.
 *
 * Change API_BASE here if your backend runs on a different port or host.
 * Do NOT hardcode the URL anywhere else — always use CONFIG.API_BASE.
 */

const CONFIG = {
  /**
   * Backend API base URL.
   * Development: http://localhost:5000
   * Production:  replace with your deployed backend URL
   */
  API_BASE: 'http://localhost:5000',
};
